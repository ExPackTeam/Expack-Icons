(function () {
    'use strict';

    function fetchFunction(svg_src, svg_name) {
      fetch(svg_src).then(function (response) {
        if (!response.ok) {
          throw new Error("icon can't load loaded");
        }
        return response.text();
      }).then(function (svgContent) {
        var svg_src = document.querySelectorAll("[data-icons='".concat(svg_name, "]"));
        svg_src.innerHTML = svgContent;
      })["catch"](function (error) {
        console.error('There was a problem with the fetch operation:', error);
      });
    }
    document.addEventListener("DOMContentLoaded", function () {
      var reactVar = "react";
      var reactIconSrc = "../svg/language_svgs/react_lang_logo.svg";
      fetchFunction(reactIconSrc, reactVar);
      var clipboardVar = "clipboard";
      var clipboardSrc = "../svg/general_icons/empty_clipboard.svg";
      fetchFunction(clipboardSrc, clipboardVar);
    });

})();
//# sourceMappingURL=expack.min.js.map
